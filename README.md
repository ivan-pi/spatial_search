# spatial_search

To include in your Fortran project use:

```
[dependencies]
spatial_search = { git = "https://github.com/ivan-pi/spatial_search.git"}
```

Examples can be found in the [`examples/`](./examples/) folder.